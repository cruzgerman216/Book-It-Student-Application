import { AlertplaceholderDirective } from './alertplaceholder.directive';

describe('AlertplaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertplaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
